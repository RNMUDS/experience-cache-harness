# 経験キャッシュ型ハーネス — 実測findings (2026-06-07)

> 中心仮説の検証: **凍結した 0.8B (`qwen3.5:0.8b`) を、ハーネス層の「経験キャッシュ」で底上げできるか。**
> キャッシュ = 検証済みの役立った経験を記憶し、つぎに瞬時に再利用する仕組み（＝人間の経験学習を、
> 計算機の完全記憶・高速検索で再現する）。再学習（fine-tuning）は一切しない。

環境: ollama 0.21 / macOS Metal / `qwen3.5:0.8b`(student, 1.0GB) / `qwen2.5-coder`(teacher) /
`nomic-embed-text`(埋め込み) / temp=0 / num_ctx=4096。全コード stdlib のみ。

---

## 1. 結論（要約）

**仮説は実証された。** 凍結 0.8B の held-out 精度は、経験キャッシュにより

| 指標 | 素の 0.8B | 経験キャッシュ | 学び |
|---|---|---|---|
| 同テーマ test (既知テーマ・未知表現) | **73.5%** | **97.1%** | +23.6pt, McNemar p=0.004 |
| LOTO test (**未知テーマ**) | **72.0%** | **94.0%** | +22.0pt, p=0.002（記憶では不可能＝真の汎化） |
| neutral 再現率（弱点クラス） | 40–43% | **93–100%** | 弱点を狙い撃ちで修復 |
| +教師エスカレーション | — | **98–100%** | コスト別建て（provenance分離） |

さらにオンライン（ゼロ知識から自己改善）では、経験が貯まるほど
**精度 92→100% / 短絡(瞬時再利用)率 13→88% / 1問あたりLLM呼数 0.87→0.12（7倍安）/ 遅延 6倍速**。

**最大の発見:** 0.8B にとって最も価値ある「キャッシュされた経験」は、かさばる検索 exemplar ではなく、
**自分の誤りから蒸留した 1 行の「インサイト（ルール）」** だった（ExpeL の知見と一致）。
ただし検索器を強く（埋め込みに）すると exemplar も同等に効く（下記§4）。

---

## 2. 設計（妥当性コントロールを最初から組み込み）

並列アドバーサリアル・レビュー（妥当性/統計/先行研究）で設計を硬化させてから実装した。
先行研究との整合: **ExpeL**(経験→インサイト蒸留+検索), **Case-Based Reasoning**(4R), **Memento**(ケースバンクで
non-parametric 学習), **KATE**(kNN few-shot>random), **MemPrompt**(誤り記憶で凍結モデル矯正), **GPTCache**(意味短絡)。

ハーネス層（決定論的制御プレーンが主役・0.8B は最後の短い判断のみ）:
- **LLMクライアント** `think:false` + 小さな `num_ctx` + 寛容パース + 構造化エラー（PoC 教訓）。
- **検索器** 文字 n-gram TF-IDF コサイン（stdlib・既定）／埋め込み（任意アーム）。
- **経験キャッシュ** add / retrieve(top-k) / **意味短絡**(類似≥τで 0 LLM 即答) / 永続化。
- **ルール蒸留** **train の誤分類からのみ**自動生成（test・ルーブリックは不可視＝漏洩防止）。
- **分類器** ルール + 検索 few-shot を組んで 0.8B 呼び出し → 低類似/曖昧なら教師へエスカレーション。

検証の生命線（記憶 vs 汎化）として実装したコントロール:
- 同一 test 上で **ベースライン再計測**（歴史値 79.2%/n=24 とは比較しない）。
- **LOTO 分割**（テーマ丸ごと未知）＝記憶では解けない真の汎化テスト。
- **新規性を検索指標と独立**に定義（difflib 編集距離比）。漏洩監査で test↔train 完全一致 0・編集比 max 0.53・検索cos max 0.25（近傍漏洩なし）。
- **1-NN-no-LLM**（コピー検出）・**ラベル反転 exemplar**（コピー vs 推論）・**oracle vs 蒸留ルール**（漏洩検出）。
- **静的ランダム vs 動的検索**を k・予算一致で比較／**ルールのみ(k=0)** で検索の寄与を分離。
- **Wilson 95%CI + 対応あり McNemar**・クラス別 precision/recall/**macro-F1**・neutral 過補正検査・**多シード**・HPは**devでのみ**調整。
- 決定論検証: temp=0 で同一プロンプト 3 回 → ビット一致。

---

## 3. アブレーション結果（10 アーム × 2 分割）

同テーマ test (n=34) / LOTO test (n=50):

| アーム | 同テーマ acc | LOTO acc | 意味 |
|---|---|---|---|
| a: 素の 0.8B（再計測ベースライン） | 73.5% | 72.0% | 出発点 |
| b: 静的ランダム k-shot | 91.2% | 92.0% | 「例があれば効く」分の上限 |
| c: 動的検索 k-shot（ルール無し） | 85.3% | 86.0% | 検索のみ（同テーマでは p=0.11 で有意でない） |
| **d: ルールのみ (k=0)** | **97.1%** | **94.0%** | **検索ゼロ、蒸留インサイト1行だけ。p<0.004** |
| e: 動的検索 + ルール | 94.1% | 94.0% | exemplar を足すと同テーマでむしろ微減 |
| g: + 教師エスカレーション | 100.0% | 98.0% | 教師の精度（コスト別建て） |
| h: 1-NN-no-LLM（コピー） | 58.8% | 64.0% | 全システム ≫ コピー → 単なる近傍コピーではない |
| i: ラベル反転 exemplar | 82.4% | 70.0% | 反転で低下（特にLOTO neutral 30%）→ 少しは exemplar を見ている |
| j: oracle ルール | 97.1% | 96.0% | 蒸留 ≈ oracle → ルールは漏洩でなく妥当な学習 |

**neutral 修復（混同行列）:** 素の 0.8B は「事実文→positive」を多発（同テーマ 6/14, LOTO 12/20 が neu→pos 誤読）。
ルールで neutral 再現率 40→100%、precision はほぼ維持（過補正わずか）。
例: 「店舗は駅から徒歩5分」→ 素は positive（"駅近=良い"と誤読）/ ルール適用後 neutral。

---

## 4. 学習曲線・オンライン・リトリーバ

**学習曲線（exemplar のみ・多シード）は非単調:**

| K | 0 | 8 | 16 | 32 | 64 | 102 |
|---|---|---|---|---|---|---|
| 同テーマ | 73.5 | **94.1** | 90.2 | 91.2 | 90.2 | 85.3 |
| LOTO | 72.0 | 88.7 | 91.3 | **92.7** | 87.3 | 86.0 |

→ **小さなキャッシュ(K≈8–32)が最適。大きくすると弱い検索が "最近傍" にノイズを引き込み精度が落ちる。**
小型モデルでは「経験は貯めるほど良い」ではなく「少数の高信号＋蒸留」が効く。

**リトリーバ比較（exemplar のみ・ルール無し, 同テーマ）:**

| 検索器 | acc | neutral再現 | 平均top類似 |
|---|---|---|---|
| 文字 n-gram | 85.3% | 71% | 0.119 |
| **埋め込み(nomic)** | **94.1%** | 93% | 0.806 |

→ exemplar が振るわなかったのは**検索が弱かったから**。埋め込み（言い換え再現が高い）にすると exemplar 単体でも 94.1% に到達し、ルールと並ぶ。
**キャッシュには2つの相補的レバー**がある: ①蒸留インサイト（安価・stdlib・弱検索でも効く）②検索 exemplar（良い検索器が要る）。

**オンライン生涯学習（ゼロ知識→自己蒸留, seeds×2, 400ステップ）:**

| 区間 | 精度 | 短絡率 | LLM/問 | 遅延 |
|---|---|---|---|---|
| 0–50 | 92.0% | 13% | 0.87 | 0.32s |
| 100–150 | 97.0% | 54% | 0.46 | 0.16s |
| 250–300 | 100% | 89% | 0.11 | 0.04s |
| 350–400 | 100% | 88% | 0.12 | 0.05s |

- **初回出現（汎化）: acc 94.7% / 1.00 LLM/問** — 未知入力にも蒸留知識が効く。
- **再来（瞬時再利用）: acc 100% / 短絡率 100% / 0.00 LLM/問** — 完全な即時想起、コストゼロ。
- 経験が貯まるほど **精度↑・コスト 7倍↓・遅延 6倍↓**。これが「経験を記憶し瞬時に活かす」の実体。

---

## 5. ハーネス工学への示唆

1. **小型の壁は "知能" でなく "知識の運び方"。** 凍結 0.8B でも、経験から蒸留した1行ルールを文脈に運ぶだけで +23pt。
2. **インサイト > 生 exemplar（弱検索時）。** 0.8B の狭い文脈には、かさばる例より蒸留知識。検索を強くすれば exemplar も活きる。
3. **キャッシュは大きいほど良い、ではない。** 小型では小数高信号が最適（学習曲線が非単調）。キュレーション/蒸留が鍵。
4. **2層キャッシュ設計が有効。** ①意味短絡（near-exact 再来 → 0 LLM 即答）②動的 few-shot/ルール（新規 → 汎化）。
5. **教師蒸留 into cache** は精度の天井を上げるが、provenance を分離して 0.8B の手柄と混ぜないこと。
6. これらは Web カスタマイズ単位（行動空間/観測整形/ガードレール/メモリ層/スキル束）の **「メモリ層」部品** として外部化できる。

---

## 6. 限界（正直な但し書き）

- **単一著者・JP感情分類・n小**（test 34/50）。CI は広い。外部データ/別著者での再現は今後。
- 文字 n-gram は言い換え再現が低い（§4）。本番の exemplar 経路は埋め込み推奨。
- ルール蒸留の定型文には帰納的バイアス（"評価語なし→neutral"）が入る。oracle≈蒸留で漏洩は否定したが、
  定型の一般性は要追検証（out_of_rule neutral を含めて検証済みだが小規模）。
- escalation/short-circuit の τ は dev で調整。データセット依存の magic number である点は不変。

## 7. 再現方法

```bash
ollama serve                         # 別ターミナル
python3 run_ablation.py              # 10アーム×2分割 → ablation_results.json
python3 run_learning_curve.py        # K別学習曲線 → learning_curve_results.json
python3 run_online.py                # オンライン生涯学習 → online_results.json
python3 run_tune.py                  # dev で組合せ最適化 → tune_results.json
python3 run_retriever_ablation.py    # 文字n-gram vs 埋め込み
```
全 LLM 応答は `llm_cache.json`（temp=0 決定論メモ化）に保存され、再実行は即時。
```
cache_harness/  llm.py retriever.py embedding_retriever.py cache.py config.py
                parsing.py prompt.py rules.py classifier.py metrics.py dataset.py experiment.py
```
